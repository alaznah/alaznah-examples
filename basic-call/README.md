# React Native SDK demo folder

Runnable app: [`examples/basic-call`](../../../examples/basic-call).

This `sdk/example/` path is reserved for a future in-package sample that ships with the published SDK.
